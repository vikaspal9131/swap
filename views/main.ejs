<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Skill Swap - Find Your Match</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

  <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">

    <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap" rel="stylesheet">
  
</head>
<body class="min-h-screen bg-[#F8F8F8] font-[Outfit]">
  <div class="container mx-auto px-4 md:px-8">

    
    <nav class="main-nav flex items-center justify-center p-4 sticky top-0 z-50 backdrop-blur-lg bg-opacity-70">
      <div class="flex justify-between items-center w-[80rem]">
        <a href="/" class="text-1xl text-black ">
         <i class="ri-blaze-line"></i> Swap
        </a> 
        <div class="flex items-center space-x-6">
          <a href="/" class="text-[14px] font-[400]">Home</a>
          <a href="/" class="text-[14px]">Request</a>
          <a href="/" class="text-[14px]">Dashbord</a>

          <% if (user) { %>
            <a href="/profile" class="text-lg font-medium text-gray-800 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-500 transition-colors duration-300">Profile</a>
            <form action="/logout" method="POST">
              <button type="submit" class="text-lg font-medium ">Logout</button>
            </form>
          <% } else { %>
            <a href="/login" class="text-[14px] hover:text-blue-500 transition-colors duration-300">Login</a>
          <% } %>
          <button id="theme-toggle" type="button" class="text-gray-600 dark:text-gray-400 hover:text-black dark:hover:text-white hover:bg-gray-200 dark:hover:bg-gray-700 rounded-lg text-sm p-2 transition-colors duration-300">
            <i class="fas fa-adjust text-lg"></i>
          </button>
        </div>
      </div>
    </nav>

    <header class="text-center my-16">
      <h1 class="text-6xl font-semibold text-black dark:text-black leading-tight">Find Your <span class="text-transparent bg-clip-text bg-gradient-to-r from-green-300 to-blue-500">Skill Match</span></h1>
      <p class="mt-4 text-xl text-gray-600 dark:text-gray-400">Unlock new possibilities through shared knowledge.</p>

      <div class="mt-12 max-w-3xl mx-auto">
        <form action="/search" method="POST">
          <div class="relative flex items-center p-3 border-2 border-gray-300 dark:border-gray-700 rounded-full bg-white dark:bg-gray-800 shadow-xl focus-within:border-blue-500 transition-all duration-300">
            <i class="fas fa-search mx-4 text-gray-400 text-xl"></i>
            <input type="text" name="query" id="searchInput" placeholder="" class="w-full bg-transparent focus:outline-none text-gray-900 dark:text-white text-lg placeholder-gray-500 input-glow" value="<%= query || '' %>" required>
            <button class="px-8 py-3 ml-4 text-white font-semibold rounded-full transition-all duration-300 btn-primary text-lg">Search</button>
          </div>
        </form>
      </div>
    </header>

    <% if (users && users.length > 0) { %>
      <h2 class="text-3xl font-bold text-black dark:text-white text-center mb-8">Matching Skills</h2>
      <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 mb-20">
        <% users.forEach(u => { %>
          <li class="p-6 border border-gray-200 dark:border-gray-700 rounded-2xl shadow-xl card-gradient transform hover:scale-105 transition-transform duration-300">
            <div class="mb-4">
              <strong class="text-blue-600 dark:text-blue-400 text-lg block mb-1">Email:</strong>
              <span class="text-gray-700 dark:text-gray-300"><%= u.email %></span>
            </div>
            <div class="mb-4">
              <strong class="text-green-600 dark:text-green-400 text-lg block mb-1">Offers:</strong>
              <span class="text-gray-700 dark:text-gray-300"><%= Array.isArray(u.skillsOffered) ? u.skillsOffered.join(", ") : "N/A" %></span>
            </div>
            <div class="mb-4">
              <strong class="text-yellow-600 dark:text-yellow-400 text-lg block mb-1">Wants:</strong>
              <span class="text-gray-700 dark:text-gray-300"><%= Array.isArray(u.skillsWanted) ? u.skillsWanted.join(", ") : "N/A" %></span>
            </div>
            <% if (user && user._id.toString() !== u._id.toString()) { %>
              <form action="/swap-request/<%= u._id %>" method="POST" class="mt-6">
                <button type="button" onclick="openPopup('<%= u._id %>', '<%= JSON.stringify(u.skillsWanted || []) %>')" class="w-full px-6 py-3 bg-gradient-to-r from-purple-600 to-pink-500 hover:from-purple-700 hover:to-pink-600 text-white rounded-full font-bold text-lg shadow-lg transform hover:scale-105 transition-all duration-300">
                  Send Swap Request
                </button>
              </form>
            <% } else if (user && user._id.toString() === u._id.toString()) { %>
              <p class="text-md text-gray-500 italic mt-6 text-center">This is you.</p>
            <% } else { %>
              <p class="text-md text-red-400 italic mt-6 text-center">Please login to send request</p>
            <% } %>
          </li>
        <% }) %>
      </ul>
    <% } else if (query && users.length === 0) { %>
      <div class="text-center my-20">
        <p class="text-3xl text-gray-500 dark:text-gray-400">No users found for "<span class="font-semibold text-black dark:text-white"><%= query %></span>".</p>
        <p class="mt-4 text-lg text-gray-500 dark:text-gray-400">Try a different skill or check your spelling.</p>
      </div>
    <% } else { %>
      <div class="text-center my-20">
        <p class="text-3xl text-gray-500 dark:text-gray-400">Start by searching for a skill you want to learn!</p>
        <p class="mt-4 text-lg text-gray-500 dark:text-gray-400">Discover a community ready to share their expertise.</p>
      </div>
    <% } %>
  </div>

  <!-- Popup Modal (no changes needed here) -->
  <div id="popupModal" class="fixed inset-0 z-50 hidden bg-black bg-opacity-70 flex items-center justify-center p-4">
    <div class="bg-white dark:bg-gray-800 p-8 rounded-xl w-full max-w-lg relative modal-content shadow-2xl border border-gray-300 dark:border-gray-700">
      <button onclick="closePopup()" class="absolute top-4 right-4 text-gray-400 hover:text-white text-3xl transition-colors duration-200">&times;</button>
      <form id="swapForm" method="POST">
        <h3 class="text-3xl font-bold mb-6 text-center dark:text-white">Send Swap Request</h3>
        <div class="mb-5">
          <label for="offeredSkillSelect" class="block text-lg mb-2 dark:text-gray-300">Choose one of your offered skills</label>
          <select name="offeredSkill" id="offeredSkillSelect" class="w-full bg-gray-100 dark:bg-gray-700 border border-gray-300 dark:border-gray-600 rounded-lg px-4 py-3 text-gray-900 dark:text-white" required>
            <% if (user && user.skillsOffered && user.skillsOffered.length > 0) { %>
              <% user.skillsOffered.forEach(skill => { %>
                <option value="<%= skill %>"><%= skill %></option>
              <% }) %>
            <% } else { %>
              <option value="" disabled selected>No skills offered</option>
            <% } %>
          </select>
        </div>
        <div class="mb-5">
          <label for="wantedSkillsSelect" class="block text-lg mb-2 dark:text-gray-300">Choose one of their wanted skills</label>
          <select name="wantedSkill" id="wantedSkillsSelect" class="w-full bg-gray-100 dark:bg-gray-700 border border-gray-300 dark:border-gray-600 rounded-lg px-4 py-3 text-gray-900 dark:text-white" required>
          </select>
        </div>
        <div class="mb-6">
          <label for="message" class="block text-lg mb-2 dark:text-gray-300">Message</label>
          <textarea name="message" id="message" rows="4" class="w-full bg-gray-100 dark:bg-gray-700 border border-gray-300 dark:border-gray-600 rounded-lg px-4 py-3 text-gray-900 dark:text-white" required placeholder="Hi! I can teach X and would love to learn Y..."></textarea>
        </div>
        <button type="submit" class="w-full bg-gradient-to-r from-blue-500 to-green-500 text-white py-3 rounded-lg font-bold text-xl hover:scale-105 transition">
          Send Swap Request
        </button>
      </form>
    </div>
  </div>


</body>
<script src="/main.js"></script>
</html>
