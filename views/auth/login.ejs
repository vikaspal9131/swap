<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login </title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap" rel="stylesheet">
</head>
<body class="dark bg-[#0A0A0A] text-white min-h-screen flex flex-col font-[Outfit] font-medium">
  <!-- Main Content -->
  <main class="flex-grow flex items-center justify-center px-4 bg-[#0A0A0A]">
    <div class="w-full max-w-md bg-[#171717] p-8 rounded-2xl shadow-xl border-[1px] border-[#424242] mt-10">
      <div class="flex justify-between items-center mb-1">
        <h2 class="text-lg font-semibold">Login to your account</h2>
        <a href="/register" class="text-sm text-white hover:underline font-[300]">Sign Up</a>
      </div>
      <p class="text-sm text-[#C8D4D4] mb-6 font-[300]">Enter your email below to login to your account</p>

      <% if (error) { %>
        <div class="bg-red-600 text-white p-2 rounded mb-4 text-sm text-center">
          <%= error %>
        </div>
      <% } %>
      <form action="/login" method="POST" class="space-y-4">
        <div>
          <label for="email" class="block text-sm font-medium text-white font-[300]">Email</label>
          <input
            type="email"
            name="email"
            id="email"
             autocomplete="off"
            placeholder="example12@gmail.com "
            required
            class="placeholder-[#C8D4D4] font-[200] w-full mt-1 px-4 py-2 rounded bg-[#212121] text-white  border-1 border-[#171717] focus:outline-none"
          />
        </div>

        <div>
          <div class="flex justify-between items-center mb-1">
            <label for="password" class="text-sm font-medium text-white font-[200]">Password</label>
            <a href="/forgot-password" class="text-sm text-white font-[300]">Forgot your password?</a>
          </div>
          <input
            type="password"
            name="password"
            id="password"
            placeholder="••••••••"
            required
            class="w-full px-4 py-2 rounded  bg-[#212121] text-white  border-1 border-[#171717]  focus:outline-none "
          />
        </div>

        <button
          type="submit"
          class="w-full bg-gray-100 text-black font-semibold py-2 rounded hover:bg-white transition"
        >
          Login
        </button>

      
      </form>
    </div>
  </main>
</body>
</html>
